# 작업 인수인계 · v2.0.0 · 2026-09-14

## 현재 구성

사용자가 10개 교과 시범 구성을 승인하고 전체 기준·원본·기존 GitHub 주소의 업데이트를 요청했다. 601개의 긴 답을 개별 생성하는 대신 13개 교과 관점과 7개 수행 규칙, 승인 사례 10개를 조합하는 구조를 적용했다. 611개에 공통 설계/UI/내보내기를 제공하며 규칙에 따른 제안과 개별 예시를 구분한다.

- `src/release.js`: v2 동작. 원문 검색·자료는 원본을 유지하며 설계와 평가·내보내기는 여기서 통합한다.
- `src/design-rules.json`: 규칙 수정 시 전체에 반영한다. 새롭게 검증한 예외는 `src/pilot-designs.json`에 추가할 수 있다. 이때 사례 선택기·pilot-source-audit.json 등 기존 예시 출처 구조도 함께 갱신하거나 확장해야 한다.
- `src/base-v1.html` 및 `src/review*.js`: 원본과 승인된 시범 UI 이력. 빌드 시 연결하며 최종 동작은 release.js가 담당한다. 원본 전체를 다시 생성하지 않는다.
- `data/app.json`: 기존 데이터 SHA256 `179dc7063393a188fba0232e736246cc89aa5bb95e5f1a50a28d17ea87763c46` 보존. `text-repairs.json`에서 수식 오류 3곳만 PDF 화면 대조 후 복원한다.

## 검증된 것과 경계

`tests/verify.cjs`로 611개 설계·6개 화면, 기준별 MD/Word/CSV와 공식 문자열, 피드백 저장·복원, 연결 보류 저장, 바구니 추가·해제, 실제 MD 다운로드, 모바일을 확인했다. 수식 복원 3곳은 원래 추출 데이터와 달라야 함을 별도로 검사한다.

자동 PDF 문자열 대조: 성취기준 610/611, 성취수준 607/611. 수식 성취수준 2곳은 화면 대조로 별도 확인했다. 일부 표의 페이지 경계 때문에 문자열 자동 대조가 되지 않는 국어·도덕 기준에는 미확인 안내와 문서 링크를 제공한다. 공식 문서 간 표현 차이가 있는 기준은 성취수준 문서와 별책 문서 양쪽을 링크한다. 표기 차이를 모델이 임의로 통일하지 말 것.

기본 영역 연결은 68개 보류. 이름 일치·겹치는 단어를 이용한 제안·교사 선택을 구분한다. 공식 단원↔영역 대응표를 확보한 것은 아니다. 모든 공통 규칙 초안을 개별 교사 검토한 것으로 소개하지 말 것.

## 다른 컴퓨터에서 이어가기

1. 이 github 폴더에서 `python src/build.py` 실행. 자료를 바꾸지 않았다면 별도 패키지나 AI 호출이 필요 없다.
2. `python -m http.server 8765 --bind 127.0.0.1` 실행 후 브라우저에서 localhost:8765 열기.
3. 검증은 README의 Playwright 안내를 따른다. APP_URL로 실제 서버 주소를 지정할 수 있다.
4. 공식 PDF가 바뀔 때만 `scripts/source_index.py` 실행. 해시별 로컬 추출 캐시를 재사용하며, 캐시는 Git 추적에서 제외한다.
5. 브라우저 저장값은 Google Drive 폴더 동기화와 별개다. 교사 편집 결과는 MD/Word/CSV 또는 기존 사회 작업 JSON으로 보관한다.
6. 현재 게시 브랜치는 achievement-criteria, 주소는 https://lo4lolo.github.io/achievement-criteria/ . 배포 전 빌드·검증, 기존 브랜치 커밋·푸시, Pages에서 버전 확인 순서로 진행한다.

프리뷰 작업 폴더의 build_preview.py는 정식 v2 빌드를 실행하고 최신 원본으로 연결한다. 이전 시범 전용 빌드는 build_pilot.py에 보존했다. 루트 오프라인용 HTML도 v2를 반영했으며 공식 PDF 링크는 github/sources를 사용한다.
